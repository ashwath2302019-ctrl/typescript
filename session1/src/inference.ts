let city = "Mumbai";
let year = 2024;

// Explicit — annotation required (no initial value)
let country: string;
let population: number;

country = "India";
population = 1400000000;

let trainig;
trainig ="hello";
trainig = 21;

//TypeScript is a superset of JavaScript 
//that adds static typing. The compiler checks types during compilation, helping catch errors before runtime